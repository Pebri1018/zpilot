# Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# Supabase → Authentication → URL configuration:
# Add redirect: http://localhost:3000/auth/callback (dev) and https://YOUR_DOMAIN/auth/callback (prod)
# Authentication → Providers → Google: enable + set Web client ID/secret from Google Cloud Console
